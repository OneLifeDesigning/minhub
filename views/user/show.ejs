<% if (user) { %>
  <div class="row">
    <div class="col-12 col-sm-8 col-md-7">
      <div class="card">
        <img src="<%= user.image %>" class="card-img-top" alt="<%= user.name %>">
      </div>
    </div>
  </div>
<% } %>